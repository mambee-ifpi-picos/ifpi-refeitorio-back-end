generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://refeitorio_q52e_user:Rq5VtDPR3ZcLQA2J6oRnrE18cVf27Yts@dpg-cev11dhgp3jjsh1c5ufg-a.oregon-postgres.render.com/refeitorio_q52e"
}

model Menu {
  id    Int    @id @default(autoincrement())
  meal  String
  day   Days
  items String

  @@map("menu")
}

model Items {
  id           Int      @id @default(autoincrement())
  name         String   @unique
  creationDate DateTime
  active       Boolean  @default(true)

  @@map("items")
}

enum Days {
  monday
  tuesday
  wednesday
  thursday
  friday
  saturday
  sunday
}
